---
"@gmc-cli/cli": patch
---

Add `gmc mcp` — a Model Context Protocol server over stdio that exposes 12 tools (doctor, accounts, products, datasources, issues, quota, reports, preflight) to AI assistants like Claude Desktop, Cursor, and VS Code Copilot.
