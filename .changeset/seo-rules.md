---
"@gmc-cli/cli": patch
---

Add 7 SEO preflight rules (`seo.*`) — title length, brand in title, differentiating attributes, description length, title≠description, brand in description, placeholder image detection. All default to `info` severity (non-gating); tune in `.gmcpreflightrc`.
